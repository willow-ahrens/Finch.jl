#!/usr/bin/env bash
set -euo pipefail

script_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
repo_root=$(cd -- "$script_dir/../.." && pwd)

exec julia --project="$repo_root/test" "$script_dir/finch_compliance_cli.jl" npy_to_binsparse "$@"
